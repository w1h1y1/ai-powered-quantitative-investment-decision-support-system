# Local development fallback.
VITE_API_BASE_URL=http://127.0.0.1:8000

# Render production: point the browser at the Static Site origin and let
# Render's /api/* rewrite proxy to the backend Web Service.
# VITE_API_BASE_URL=https://aiquant-frontend.onrender.com
